# vim: softtabstop=2 expandtab shiftwidth=2 tabstop=2
general:
  artifacts:
    - "target/*.jar"
dependencies:
  cache_directories:
    - "~/.m2"
compile:
  override:
    - mvn clean package
test:
  override:
    - mvn integration-test install
